source 'https://rubygems.org'

gem 'json' # for parsing and building api response data
gem 'rest-client'  # framework for calling APIs
gem 'figaro' # dependency management
require 'cgi'  # handles URL encoding for SF rest API calls
require 'optparse'  # framework for applciation CLI
require 'csv' # read/write library for CSV's
